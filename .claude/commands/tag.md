---
description: Audit and fix tags/frontmatter across the vault
---
Audit YAML frontmatter on all notes (skip .obsidian/, .claudian/, .claude/). Ensure title + tags exist in inline array style, reuse the existing tag vocabulary, and report changes. Target: $ARGUMENTS (whole vault if empty).
